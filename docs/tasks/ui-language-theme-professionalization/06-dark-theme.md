# Task 06: Intentional Dark Theme Refinement

## Objective
Refine the Dark Theme into a designed, intentional dark studio workspace (not an inverted CSS filter), maintaining high contrast, clear surface hierarchy, readable inputs, and subtle slate borders.

## Scope
- `apps/web/src/app/globals.css`
- Dark mode theme classes (`.dark`, `[data-theme="dark"]`)
- Surface hierarchy: Deep Canvas (`#090D16`), Card Elevation (`#111827`), Subtle Borders (`#1E293B`), Primary Text (`#F8FAFC`), Secondary Text (`#94A3B8`).

## Status
IN PROGRESS
