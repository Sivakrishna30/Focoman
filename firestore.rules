rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, direct client mutations are denied.
    // Privileged business mutations execute via Next.js Server Actions using Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
