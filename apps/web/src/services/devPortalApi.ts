import { Task } from '@focoman/types';

/**
 * Focoman Developer Portal & Internal Diagnostic Helper Service
 */

export const devPortalApi = {
  getSystemTasks: async (): Promise<Task[]> => {
    return [];
  }
};
